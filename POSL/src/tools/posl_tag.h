#pragma once

#include <string>
#include <memory>



/*
class POSL_Tag
{
    public:
        POSL_Tag(int _id, int _type_tag, ConnectionType _connection_type);

        void updateTag(int _connection_tag);
        bool compatible(POSL_Tag & other);

        int GetConnectionTag() { return connection_tag; }

    private:
        int ID;
        int type_tag;
        ConnectionType connection_type;
        int connection_tag;
};
*/
